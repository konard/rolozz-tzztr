<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="2026-06-25-create-users-table" author="your_name">
        <createTable tableName="users">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="username" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="password" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="wb_token" type="VARCHAR(1000)">
                <constraints nullable="true"/>
            </column>

            <column name="role" type="VARCHAR(50)" >
                <constraints nullable="false"/>
            </column>

            <column name="is_verified" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="create_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="update_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="2026-06-28-add-user-id-to-wb-card" author="system">
        <addColumn tableName="wb_card">
            <column name="user_id" type="UUID">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>

    <changeSet id="2026-06-28-add-foreign-key-user-card" author="system">
        <addForeignKeyConstraint
                baseTableName="wb_card"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_wb_card_user"
                onDelete="CASCADE"/>
    </changeSet>

    <!--
        Переход на Keycloak: пароли хранятся и проверяются только в Keycloak,
        поэтому колонка password в профиле сервиса больше не нужна.
        Предусловие позволяет применить набор изменений и к базе,
        в которой колонка уже отсутствует.
    -->
    <changeSet id="2026-08-05-drop-users-password" author="keycloak-migration">
        <preConditions onFail="MARK_RAN">
            <columnExists tableName="users" columnName="password"/>
        </preConditions>
        <comment>Удаление колонки password: учётные данные перенесены в Keycloak</comment>
        <dropColumn tableName="users" columnName="password"/>
    </changeSet>

</databaseChangeLog>